#include<stdio.h>
#include<string.h>
int cnt=0;
char *input(char **p)
{
int i,j;
cnt++;
for(i=0;i<cnt;i++)
p[i]=realloc(p,cnt*)
}
main()
{
char **p=0;
int n1,n2;
p=malloc(sizeof(char*));
p=input(p);
}

